name: main_workflow
on: push

jobs:
  # linter-job:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout
  #       uses: "actions/checkout@v4"

  #     - name: Configura Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: "18"

  #     - name: Instal·la les dependencies
  #       run: npm install

  #     - name: Linter
  #       uses: "github/super-linter@v4"

  #   env:
  #     DEFAULT_BRANCH: main
  #     GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
  #     LINTER_RULES_PATH: /
  #     JAVASCRIPT_ES_CONFIG_FILE: .eslintrc.json
  #     VALIDATE_JAVASCRIPT_ES: true

  cypress-job:
    runs-on: ubuntu-latest
    #needs: linter-job

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configura Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          start: npm run dev
          wait-on: "http://localhost:3000"
          browser: chrome
          summary-title: "Cypress Test Results"
        continue-on-error: true

      - name: Guardar summary com un artifact
        run: |
          mkdir -p artifacts
          echo "$GITHUB_STEP_SUMMARY" > artifacts/cypress-summary.txt

      - name: Pujar artifact
        uses: actions/upload-artifact@v4
        with:
          name: cypress-summary
          path: artifacts/cypress-summary.txt
